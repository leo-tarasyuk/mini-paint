export type User = {
    email: string;
    password: string;
}

export type Picture = {
    img: string;
    name: string;
};

export type Pictures = {
    img: string;
    name: string;
    user: string;
};